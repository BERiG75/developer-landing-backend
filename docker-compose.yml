services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: developer-landing-backend

    ports:
      - "80:80"

    env_file:
      - .env

    volumes:
      - ./storage:/app/storage

    restart: unless-stopped