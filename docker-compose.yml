services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: developer-landing-backend

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./storage:/app/storage

    restart: unless-stopped